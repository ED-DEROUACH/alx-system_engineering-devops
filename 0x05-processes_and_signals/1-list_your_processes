#!/usr/bin/env bash
# This script displays a list of currently running processes.

# Use the ps command to list all processes, including those without a TTY, in a user-oriented format.
ps aux --forest
