#!/usr/bin/env bash

# This script uses the 'ip' command to display active IPv4 IPs and filters out loopback and link-local IPs.

active_ips=$(ip -4 addr show | grep -oP 'inet \K[\d.]+')

# Loop through the active IPs and display them
for ip in $active_ips; do
    echo $ip
done

